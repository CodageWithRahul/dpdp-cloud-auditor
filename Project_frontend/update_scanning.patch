
*** Begin Patch
*** Update File: frontend/js/scanning.js
@@
-const REPORT_BASE = ${BASE_URL}/api/reports; 
-const HISTORY_URL = ${BASE_URL}/api/scanner/scan/history/; 
+const REPORT_BASE = ${BASE_URL}/api/reports; 
+const HISTORY_URL = ${BASE_URL}/api/scanner/scan/history/; 
+const SCAN_ENDPOINT = ${BASE_URL}/api/scanner/scan; 

@@
-const refreshLogs = async () => {
-  if (!jobId) return;
-  try {
-    const logsUrl = new URL(LOGS_PATH);
-    logsUrl.searchParams.set('scan_job_id', jobId);
-    logsUrl.searchParams.set('limit', '100');
-    const response = await fetchWithAuth(logsUrl.toString(), { method: 'GET' });
-    if (!response.ok) {
-      throw new Error('Unable to load scan logs.');
-    }
-    const payload = await response.json().catch(() => null);
-    const entries = normalizeLogPayload(payload);
-    entries.forEach((entry) => appendLog(entry));
-  } catch (error) {
-    console.warn('Failed to load logs', error);
-  }
-};
+const refreshLogs = async () => {
+  if (!jobId) return;
+  try {
+    const logsUrl = ${SCAN_ENDPOINT}//logs/?limit=100;
+    const response = await fetchWithAuth(logsUrl, { method: 'GET' });
+    if (!response.ok) {
+      throw new Error('Unable to load scan logs.');
+    }
+    const payload = await response.json().catch(() => null);
+    const entries = normalizeLogPayload(payload);
+    entries.forEach((entry) => appendLog(entry));
+  } catch (error) {
+    console.warn('Failed to load logs', error);
+  }
+};

@@
-const stopScan = async () => {
-  if (!jobId || stopInFlight || scanComplete) return;
-  stopInFlight = true;
-  stopButton && (stopButton.disabled = true);
-  try {
-    const response = await fetchWithAuth(STOP_PATH, {
-      method: 'POST',
-      body: JSON.stringify({ scan_job_id: jobId, cloud_account_id: accountId }),
-    });
-    const payload = await response.json().catch(() => null);
-    if (!response.ok) {
-      throw new Error(payload?.detail || payload?.message || 'Unable to cancel the scan.');
-    }
-    setMessage('Stop request accepted. The scanner will halt shortly.', 'success');
-  } catch (error) {
-    setMessage(error?.message || 'Failed to send stop request.', 'error');
-  } finally {
-    stopInFlight = false;
-    if (!scanComplete && stopButton) {
-      stopButton.disabled = false;
-    }
-  }
-};
+const stopScan = async () => {
+  if (!jobId || stopInFlight || scanComplete) return;
+  stopInFlight = true;
+  stopButton && (stopButton.disabled = true);
+  try {
+    const response = await fetchWithAuth(${SCAN_ENDPOINT}//stop/, {
+      method: 'POST',
+      body: JSON.stringify({ cloud_account_id: accountId }),
+    });
+    const payload = await response.json().catch(() => null);
+    if (!response.ok) {
+      throw new Error(payload?.detail || payload?.message || 'Unable to cancel the scan.');
+    }
+    setMessage('Stop request accepted. The scanner will halt shortly.', 'success');
+  } catch (error) {
+    setMessage(error?.message || 'Failed to send stop request.', 'error');
+  } finally {
+    stopInFlight = false;
+    if (!scanComplete && stopButton) {
+      stopButton.disabled = false;
+    }
+  }
+};

@@
-    if (response.ok) {
-      reportChecked = true;
-      if (viewReportButton) {
-        viewReportButton.disabled = false;
-      }
-      setMessage('Report ready. Redirecting you to the findings now...','success');
-      window.location.href = buildReportUrl();
-      return;
-    }
+    if (response.ok) {
+      reportChecked = true;
+      if (viewReportButton) {
+        viewReportButton.disabled = false;
+      }
+      setMessage('Report ready. Redirecting you to the findings in 5 seconds...','success');
+      setTimeout(() => {
+        window.location.href = buildReportUrl();
+      }, 5000);
+      return;
+    }
*** End Patch
